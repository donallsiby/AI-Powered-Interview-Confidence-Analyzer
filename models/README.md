# Models Directory

This directory stores trained model weight serializations, scalers, and pipelines.

## Serialized Files

- `confidence_model.pkl` - Serialized Random Forest Regressor trained on audio and NLP features.
- `scaler.pkl` - Standard scaler used to normalize acoustic features prior to inference.
